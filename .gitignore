# Dependency directories
# Reason: dependencies are installed from lockfile in CI; never commit `node_modules`.
node_modules/

# Build outputs
# Reason: built artifacts are generated by CI/Netlify and would create noisy diffs.
dist/

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# OS/editor files
.DS_Store
.idea/
.vscode/

# Env files
# Reason: can contain secrets; should be configured in Netlify/GitHub Actions instead.
.env
.env.*

